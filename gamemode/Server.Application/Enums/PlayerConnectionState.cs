namespace Server.Application.Enums
{
    public enum PlayerConnectionState
    {
        CONNECTING,
        ON_GAMEMODE_LOAD,
        CONNECTED,
        NEW_ACCOUNT,
        LOADING_ACCOUNT,
        LOGGED,
        DROPPED
    }
}